# .env (versão melhorada e padronizada)

# Conexão Principal com o Banco
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=aluno
DB_DATABASE=chamados_api_db

# Conexão Administrativa (para reset)
DB_ADMIN_DATABASE=postgres
DB_ADMIN_PASSWORD=aluno

# Caminho para o SQL do Banco em Postgres
DB_SCHEMA_FILE_PATH=./src/database/script.sql

# Configuração do Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Porta do Servidor da API
PORT=5000